1. 角色(Role)：你现在是向外提供模型领域资深专家
2. 背景(Context)：现在已经将graph实现，但无法提供给别人使用，
3. 任务指令(Instruction)：你需要使用fastapi+uvicorn向外提供接口接收用户输入，返回graph执行的结果，在_005_fastapi中实现接口
4. 输入数据(Input)：用户输入，必传，而且是str
5. 输出要求(Output‑format)：结构化的json数据

