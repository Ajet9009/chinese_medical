# 草本通 · 使用手册

> 面向**使用者 / 管理员**的实操指南。自然语言问方剂、本草、证候 → 图谱 + 文献 → 可拒答的答案。  
> 开发/架构见 `README.md` 与 `docs/系统架构.md`。本手册只讲「怎么用」。  
> 产品是中医问答，不是电网运维；前端在 **5174**，不要和电网项目的 5173 搞混。

---

## 目录

1. [快速上手](#1-快速上手)
2. [智能问答（💬）](#2-智能问答)
3. [知识库（📚）](#3-知识库)
4. [知识治理（🧭，管理员）](#4-知识治理管理员)
5. [系统管理（⚙️，管理员）](#5-系统管理管理员)
6. [角色与权限](#6-角色与权限)
7. [常见问题排查](#7-常见问题排查)
8. [快捷操作速查](#8-快捷操作速查)
9. [换设备 / 给接收方](#9-换设备--给接收方)

---

## 1. 快速上手

| 项 | 值 |
|---|---|
| 前端 | `http://localhost:5174` |
| 接口文档 | `http://localhost:8000/docs` |
| 健康检查 | `http://localhost:8000/health`（含 `redis`、`degraded`） |
| 默认账号 | **admin / admin123**（管理员） |
| 普通角色 | user（问答、知识库浏览/上传视配置；不能进管理与治理） |

**登录后**：顶栏「问答 / 知识库 / 知识治理(admin) / 管理(admin)」。右侧可进个人资料改科室、密码。

> 图谱里要有方剂节点，知识库文档要 **解析 + 向量化** 后才会进文献检索。演示稿：`corpus/sijunzi.md`、`corpus/xiaochaihu.md`。

---

## 2. 智能问答

最核心页面。像聊天一样问中医问题。

### 提问

- 底部输入框（如「四君子汤有什么功效？」），发送或 Enter。Shift+Enter 换行。
- 答案**逐字流式**。左侧可看到图节点进度（消解指代、识别意图、检索文献等）。
- 非中医问题（如股价）会走通用回答或拒答，不编造图谱事实。

### 答案区

- **引用来源**：命中的文献块（文档名 + 片段）。
- **依据有限**：文献弱、图谱仍有货时，可能带「依据有限，仅供参考。」
- **拒答**：图谱与文献都无依据时固定句「均未查到…请改问方剂、药材或证型。」
- **相关追问**：点胶囊即可接着问（多轮会做指代消解，「它由哪些药组成？」）。
- **操作**：重新生成 / 复制 / 导出 / 收藏 / 👍👎。

### 对话管理（左侧栏）

- 「+ 新对话」；搜索、重命名、删除、批量删。
- 多轮会带上压缩后的近期历史。条数由 Redis `LTRIM` 管；真正进模型的长度由 **ContextCompressor** 的 `token_budget` 管，不是简单「最近 N 条」。

### 点踩回流

👎 后，管理员可在「管理 → 反馈」把该问句 **标为黄金集**（待标注，`expect` 为空）。用于回归，不是自动改知识。

---

## 3. 知识库

顶栏「📚 知识库」。

1. 上传 `.md` / `.txt` / `.pdf` 等。类型选 **方剂 / 本草 / 典籍 / 医案 / 其他**。科室可填（空=公开）；授权角色可空。
2. 对文档点 **解析**（切块）→ **向量化**（写入文献 FAISS）。只上传不向量化，问答检索不到。
3. 状态列应变为已向量化；块数 > 0。
4. **版本信息**：第一次上传只有「现行 v1」，版本列表可以没有归档行。同名再传才会把旧文件进 `版本`。不要误判为没传上。
5. 管理员可回滚版本、改科室权限。

管理页「系统运行状态 → 文献 RAG → 重建文献索引」：把**已经向量化**的块重写成 `docs.index`，不重新解析，也不重建 Neo4j / 实体 FAISS。

---

## 4. 知识治理（管理员）

顶栏「🧭 知识治理」。给已入库文档补：责任人、适用区域、有效期/长期有效、复审间隔。

- **扫描**：过期、缺字段等会生成问题单。
- **审核**：通过 / 驳回。过期文档会从检索里剔除（`blocked_ids`）。

这不是「知识自进化」（不会根据点踩自动改图谱）。

---

## 5. 系统管理（管理员）

顶栏「⚙️ 管理」，标签页：

| 标签 | 做什么 |
|---|---|
| 👍 反馈 | 赞/踩列表；证据缺口；**标为黄金集** |
| 📝 Prompt | 只读：节点模板来自 Langfuse 还是本地 fallback |
| 💚 系统运行状态 | Redis / Neo4j 节点数 / 两套 FAISS / 文献块数 / 降级打点 / 黄金集条数。**点击卡片可放大**；鼠标滑过标签与卡片有过渡 |
| 🧪 评测 | 黄金集问句、类别、期望关键词 |
| 👥 用户 | 新建、改角色/科室、禁用、重置密码。仅 user / admin，不开放自行注册 |
| 📋 操作日志 | 登录、改密、用户管理等；可按时间范围、用户、类型查询 |

### 系统运行状态卡片怎么读

- **Redis**：会话热缓存通不通。挂了问答改走 SQLite。
- **Neo4j**：`Formula`/`Herb` 等是**库存数量**，不是正确率。
- **FAISS**：实体标准化索引（口语对齐标准名）。
- **文献 RAG**：知识库向量。与上一张不是同一文件。
- **降级打点**：备胎被踩的次数（进程内存，重启清零）。
- **黄金集**：已标注 / 待标注。

### 评测怎么跑（开发）

```bash
python scripts/eval_golden.py
```

离线关键词全命中才算过。`--live` 打本机 `/ask`，不要进 CI。

---

## 6. 角色与权限

| 功能 | admin | user |
|---|:---:|:---:|
| 智能问答 / 会话 / 收藏 | ✅ | ✅ |
| 知识库列表（本科室 + 公开） | ✅ | ✅ |
| 知识库上传 | ✅ | ✅（写入本人科室） |
| 解析 / 向量化 / 删除 / 回滚 | ✅ | ❌ |
| 知识治理 | ✅ | ❌ |
| 管理（反馈/用户/评测/运行状态） | ✅ | ❌ |

文档 ACL：空科室=公开；非空则仅同科室 + admin。`allowed_roles` 空则科室内全员。

---

## 7. 常见问题排查

**Q：问答 500 / 一直转圈？**  
A：先打开 `http://localhost:8000/health`。不通就重启 FastAPI。前端必须是 **5174**。硬刷新 Ctrl+F5。

**Q：答案总是「未查到」？**  
A：保守拒答。检查 Neo4j 是否有 `Formula`/`Herb`；知识库是否已向量化；问句是否具体到方剂/药材/证型。

**Q：上传了小柴胡汤但版本信息是空的？**  
A：首次上传不归档。看文献列表状态和块数；版本弹窗应有「现行 v1」。

**Q：上传后问答仍只命中四君子？**  
A：确认已解析+向量化。可点「重建文献索引」。实体 FAISS 与文献 FAISS 搞混时，口语对齐会偏。

**Q：滑过管理标签没动画 / 卡片点不开？**  
A：确认已拉到含 Admin 卡片放大的提交；硬刷新。

**Q：降级打点一直涨 redis？**  
A：Redis 没起或 `REDIS_URL` 错。问答仍可用（SQLite），但热路径在走备胎。

**Q：换电脑 clone 后没登录、没文档？**  
A：`.env` 和 `data/` 不在 git。见 [§9](#9-换设备--给接收方)。

---

## 8. 快捷操作速查

| 想做什么 | 去哪 |
|---|---|
| 问方剂功效/组成 | 💬 问答 |
| 看历史对话 | 💬 左侧栏 |
| 上传本草/方剂文献 | 📚 知识库 → 解析 → 向量化 |
| 补有效期 | 🧭 知识治理 |
| 把踩过的题做成考卷 | ⚙️ 反馈 → 标为黄金集 |
| 看 Redis/图/索引是否活着 | ⚙️ 系统运行状态（可点卡片放大） |
| 离线回归 | `python scripts/eval_golden.py` |
| 改自己科室 | 顶栏用户 → 个人资料 |

---

## 9. 换设备 / 给接收方

### 9.1 Git

远程 `https://github.com/Ajet9009/chinese_medical.git`，分支 `main`。clone 后确认 HEAD 含知识库与评测飞轮（`docs/系统架构.md` 应存在）。

### 9.2 git 里没有的东西

| 路径 | 处理 |
|---|---|
| `.env` | 从旧机拷贝，或从 `.env.example` 填密钥与本机路径 |
| `data/conversations.sqlite` | 会话、用户、知识库登记；不拷则空库，会 seed admin |
| `data/docs/` | 已上传原件 |
| `data/faiss/docs.*` | 文献索引；可在知识库重新向量化或管理页重建 |
| 实体 `entities.index` / `entities.json` | 从 Neo4j 重建或拷贝，写入 `FAISS_*` |
| `frontend/node_modules` | `npm --prefix frontend install` |

### 9.3 新机启动

```bash
# conda 环境名用 grid-qa（与电网项目可共用解释器，端口不要共用）
conda activate grid-qa
pip install -r requirements.txt
npm --prefix frontend install
docker compose up -d redis   # 或 REDIS_URL 指到已有 6379
# 启动本机 Neo4j 4.4
python -m uvicorn _005_fastapi.main:app --host 127.0.0.1 --port 8000
npm --prefix frontend run dev   # 5174
```

测一下：`pytest tests -q`。

### 9.4 Cursor 约定

- 不要擅自 `codegraph init`。  
- 不要改电网项目 5173。  
- 整理记忆文档时必须写清 ContextCompressor（`token_budget` / `fit_budget` / LLM 摘要）与 Redis LTRIM 条数裁剪的区别。
