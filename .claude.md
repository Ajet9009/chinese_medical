# 已迁移（本文件不是 Agent 注入点）

`.claude.md` **不会**被 Claude Code 或 Cursor 当作项目手册加载。

| 工具 | 读取的文件 |
|------|------------|
| Cursor Agent / CLI | 根目录 `AGENTS.md`；Cursor 也会读根目录 `CLAUDE.md` |
| Claude Code | 根目录 `CLAUDE.md`（其中 `@AGENTS.md` 引入正文） |

请只改 **`AGENTS.md`**。不要再往本文件追加规范。
