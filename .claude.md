# 草本通 — 中医药知识图谱问答系统

## 1. 项目概述

将非结构化中医药文本转化为结构化知识图谱，结合 LLM + LangGraph 实现自然语言问答。

**核心链路**：用户问题 → 意图识别 → 实体抽取 → 向量匹配 → Cypher 生成 → 图谱查询 → 回答生成

---

## 2. 目录结构

```
_000_demo/                      # 验证脚本
├── demo_api.py                 #   API 调用验证（交互/批量/单次）
├── streamlit_app.py            #   (旧) Streamlit 前端
├── faiss_search_example.py     #   FAISS 检索示例

_001_crawler/                   # 数据爬取
├── fangji_index_crawler.py     #   方剂索引爬虫
├── zhongyao_index_crawler.py   #   中药索引爬虫
├── detail_crawler.py           #   详情页爬虫

_002_extract_information/       # 知识抽取
├── kg_extractor.py             #   LLM 实体关系抽取（DeepSeek + LangChain）
├── build_kg.py                 #   规则层+LLM层 合并构建图谱

_003_create_neo4j_database/     # Neo4j 入库 + FAISS
├── import_neo4j.py             #   实体/关系批量导入 Neo4j
├── build_faiss_index.py        #   从 Neo4j 构建 FAISS 向量索引

_004_langgraph_more_nodes/      # LangGraph 图节点（核心）
├── state.py                    #   GraphState 定义 + KG实体/关系常量
├── graph.py                    #   图编排（7节点 + 条件路由）
├── intent_recognition.py       #   节点1: 意图识别（中医/普通）
├── entity_extraction.py        #   节点2: 六类中医实体抽取
├── entity_normalization.py     #   节点3: FAISS+BGE 向量匹配标准实体
├── cypher_generation.py        #   节点4: LLM生成Cypher + EXPLAIN校验修正
├── cypher_executor.py          #   节点5: 执行Cypher，结果去重格式化
├── answer_generation.py        #   节点6: 结合KG结果生成最终回答
└── general_response.py         #   节点7: 非中医问题直接回答

_005_fastapi/                   # FastAPI 服务
└── main.py                     #   /ask (普通) + /ask/stream (SSE流式)

_006_streamlit/                 # Streamlit 前端
├── app.py                      #   流式进度 + 原生聊天气泡
├── run.py                      #   启动器 (subprocess)
└── .streamlit/config.toml      #   浅色主题配置

common/                         # 公共模块
├── neo4j_manager.py            #   Neo4j 连接/导入/校验/执行/元数据
├── faiss_vector_store.py       #   FAISS 向量存储封装
├── langfuse_manager.py         #   Langfuse 客户端：初始化/采样/trace/flush
├── sanitizer.py                #   PII 脱敏（手机号/身份证/邮箱）
├── export_neo4j_metadata.py    #   导出图 schema → data/neo4j_metadata.json
└── .env                        #   环境变量（API Key、DB连接、模型路径）

data/                           # 数据目录
├── neo4j/
│   ├── entities.json           #   标准实体（供 FAISS 对照）
│   └── relations.json          #   标准关系
└── neo4j_metadata.json         #   图 schema（供 LLM 生成 Cypher）

tests/                          # 测试
├── test_langfuse_integration.py #   Langfuse 集成测试
├── test_intent_recognition.py   #   意图识别测试
└── test_general_response.py     #   通用回答测试
```

---

## 3. LangGraph 图流程

```
user_question
      │
      ▼
intent_recognition (LLM) ──→ is_zhongyi_intent?
      │                          │
      │ False                    │ True
      ▼                          ▼
general_response (LLM)    entity_extraction (LLM)
      │                   抽取六类实体: symptom/disease/formula/herb/effect/source
      │                          │
      │                          ▼
      │                   entity_normalization (FAISS+BGE)
      │                   user_input_* → matched_* (top3+骤降截断+≥0.6)
      │                          │
      │                          ▼
      │                   cypher_generation (LLM)
      │                   基于 matched_* + neo4j schema → 生成 Cypher
      │                   EXPLAIN 校验（最多3次修正）
      │                          │
      │                          ▼
      │                   cypher_executor (无LLM)
      │                   执行 Cypher → 去重去噪 → neo4j_answer
      │                          │
      │                          ▼
      │                   answer_generation (LLM)
      │                   neo4j_answer + user_question → final_answer
      │                          │
      ▼                          ▼
    END ←──────────────────── END
```

---

## 4. State 关键字段

| 字段 | 类型 | 来源节点 | 说明 |
|------|------|----------|------|
| `user_question` | `str` | 入口 | 用户原始问题 |
| `intent` | `Literal["tcm","general"]` | intent_recognition | 意图分类 |
| `is_zhongyi_intent` | `bool` | intent_recognition | 路由判断 |
| `user_input_symptoms` | `list[str]` | entity_extraction | 六类用户实体 |
| `matched_symptoms` | `list[MatchedEntity]` | entity_normalization | 匹配到的标准实体 |
| `cypher_queries` | `list[str]` | cypher_generation | 生成的查询语句 |
| `neo4j_answer` | `str` | cypher_executor | 图查询结果（精简文本） |
| `final_answer` | `str` | answer_generation / general_response | 最终回答 |

---

## 5. 关键技术细节

### 向量匹配（entity_normalization）
- 模型：BGE-large-zh-v1.5（`EMBEDDING_MODEL_PATH`）
- 搜索时加类型前缀 `"Symptom: 咳嗽"` 提高同类精度
- 动态阈值：骤降截断（`score[i]/score[i-1] < 0.6`）+ 绝对最低 0.6

### Cypher 生成 + 校验（cypher_generation）
- LLM 参考实时 Neo4j schema（节点属性 + 关系 pairs）
- 输出 JSON 数组，逐条 `EXPLAIN` 校验
- 失败时错误信息喂回 LLM 修正，最多 3 次

### 元数据（neo4j_metadata.json）
- 通过 `Neo4jManager.get_schema_metadata()` 实时获取
- 导出脚本：`python common/export_neo4j_metadata.py`

### 可观测性（Langfuse 全链路追踪）
- **零侵入**：通过 `langfuse.langchain.CallbackHandler` 自动拦截 LLM 调用和 LangGraph 节点
- 一次 API 请求 = 一条 Trace，每个节点 = 一个 Span，每次 LLM 调用 = 一个 Generation
- 管理模块：`common/langfuse_manager.py` — 客户端初始化、采样、flush
- 脱敏模块：`common/sanitizer.py` — 手机号/身份证/邮箱 → `[REDACTED]`
- 环境变量控制：
  - `LANGFUSE_ENABLED` — 全局开关（默认 true）
  - `LANGFUSE_SAMPLE_RATE` — 采样率 0~1（默认 0.1）
  - `LANGFUSE_HOST` — 私有化部署地址（默认 cloud.langfuse.com）
  - `LANGFUSE_UNMASK` — 设为 true 跳过脱敏

---

## 6. 环境配置

- **Python**: 3.10+（conda 环境按项目名，本仓库可用 `chinese_medical` 或现有 `ctm_kg`）
- **环境变量** (`common/.env`):
  - `MODEL_API_KEY` / `MODEL_BASE_URL` / `MODEL_NAME` — LLM
  - `NEO4J_URI` / `NEO4J_USER` / `NEO4J_PASSWORD` — 图数据库
  - `EMBEDDING_MODEL_PATH` — BGE 模型路径
  - `FAISS_INDEX_PATH` / `FAISS_METADATA_PATH` — 向量索引
  - `LANGFUSE_SECRET_KEY` / `LANGFUSE_PUBLIC_KEY` — Langfuse 认证
  - `LANGFUSE_HOST` — Langfuse 服务地址
- **可选**：`MATCH_MIN_SCORE` / `MATCH_TOP_K` / `MATCH_DROP_RATIO` / `LANGFUSE_ENABLED` / `LANGFUSE_SAMPLE_RATE` / `SERVICE_VERSION`

---

## 7. 启动手册（按顺序执行）

> 仓库路径：`D:\chinese_medical`

### 第 1 步：启动 Redis

```bash
docker compose up -d redis
```

若 6379 已被占用，将 `.env` 的 `REDIS_URL` 指向已有 Redis。Langfuse 仍可用原有 `milvus_data` 栈。

### 第 2 步：启动 Neo4j 图数据库

```bash
cd E:\devapp\neo4j-community-4.4.41\bin
neo4j.bat console        # 前台启动（看日志）
# 或
neo4j.bat start          # 后台启动（需已安装 Windows 服务）
```

验证：`cypher-shell.bat -u neo4j -p <密码> "RETURN 1"` 能返回 1。

> 密码见 `common/.env` 的 `NEO4J_PASSWORD`。

### 第 3 步：启动项目

```bash
cd D:\chinese_medical

# 终端 1：API 服务（端口 8000）
python -m _005_fastapi.main

# 终端 2：Vue Chat（端口 5174，避开电网项目 5173）
npm --prefix frontend run dev
```

访问：
- 前端页面：`http://localhost:5174`
- API 文档：`http://localhost:8000/docs`
- Langfuse：`http://localhost:3000`
- 遗留 Streamlit：`python _006_streamlit/run.py`

### 测试/验证
```bash
# API 调用
python _000_demo/demo_api.py "四君子汤有什么功效？"

# 单节点测试（每个文件都有 main()）
python -m _004_langgraph_more_nodes.intent_recognition
python -m _004_langgraph_more_nodes.entity_extraction
python -m _004_langgraph_more_nodes.cypher_generation

# Langfuse 集成测试
pytest tests/test_langfuse_integration.py -v
```

### 常见问题

| 症状 | 原因 | 解决 |
|------|------|------|
| 连不上 `localhost:7687` | Neo4j 未启动 | 执行第 2 步 |
| 连不上 `localhost:8000` | API 未启动 | 执行第 3 步终端 1 |
| Langfuse 无数据 | 采样率太低或未启用 | 检查 `.env` 的 `LANGFUSE_SAMPLE_RATE` |
| 白底白字 | Streamlit 主题问题 | 已用 `.streamlit/config.toml` 强制浅色 |

---

## 8. 可观测性（Langfuse）

### 架构

Langfuse 通过 LangChain `CallbackHandler` 实现零侵入追踪：
- `common/langfuse_manager.py` — 客户端封装：初始化、采样、trace 创建、flush
- `common/sanitizer.py` — PII 脱敏（手机号/身份证/邮箱 → `[REDACTED]`）
- `_005_fastapi/main.py` — 在 lifespan 中初始化，请求中注入 callback

### Trace 结构

```
POST:/ask (Trace, metadata: request_id + session_id + version)
├── intent_recognition (Span)
│   └── ChatOpenAI.invoke (Generation: model + tokens + latency)
├── entity_extraction (Span)
│   └── ChatOpenAI.invoke (Generation)
├── entity_normalization (Span)   ← 纯计算，无 LLM
├── cypher_generation (Span)
│   └── ChatOpenAI.invoke (Generation)  ← 生成
│   └── ChatOpenAI.invoke (Generation)  ← 修正（最多3次）
├── cypher_executor (Span)        ← 纯 Neo4j，无 LLM
├── answer_generation (Span)
│   └── ChatOpenAI.invoke (Generation)
```

### 采样与容错

- 默认 10% 采样（`LANGFUSE_SAMPLE_RATE=0.1`）
- 未命中采样 → 完全不创建 Trace，零开销
- 初始化失败 → 全局禁用，不影响业务
- 密钥缺失 → 自动降级，不抛异常
- 所有 Langfuse 操作 try-except + logging warning

### 环境变量

```bash
LANGFUSE_SECRET_KEY=sk-lf-...     # 必填
LANGFUSE_PUBLIC_KEY=pk-lf-...     # 必填
LANGFUSE_HOST=https://cloud.langfuse.com  # 私有部署时修改
LANGFUSE_ENABLED=true             # 全局开关
LANGFUSE_SAMPLE_RATE=0.1          # 采样率
LANGFUSE_UNMASK=false             # 设为 true 跳过脱敏
SERVICE_VERSION=1.0.0             # trace metadata
```
