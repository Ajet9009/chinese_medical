# 草本通 · 架构总览（代码级 mermaid）

> **版本**：`7b1da4f` 后补文档，2026-09-12 ｜ **仓库**：chinese_medical  
> **技术栈**：Vue3(:5174) + FastAPI(:8000) + SQLite + Redis + Neo4j 4.4 + 双 FAISS（实体 / 文献）+ LangGraph + Langfuse  
> 节点命名约定：`文件.py::函数()` / 表名 / 开关名  
> 产品思路对齐 grid-qa；**不抄**电网壳（两票、孪生、SCADA、Prometheus 全家桶、知识自进化）。

---

## 图 1 · 系统总览（分层 + 主数据流）

```mermaid
flowchart TB
subgraph FE["🖥️ frontend/src/views"]
    Chat["Chat.vue /chat<br/>streamAsk SSE"]
    DocV["Documents.vue /documents"]
    Gov["KnowledgeGovernance.vue<br/>admin"]
    Adm["Admin.vue /admin<br/>反馈·Prompt·系统运行状态·评测"]
    Prof["Profile.vue"]
end

subgraph GW["🌐 _005_fastapi"]
    Main["main.py<br/>/ask /ask/stream<br/>/conversations /auth /admin"]
    Know["knowledge.py<br/>/document/*<br/>/knowledge-governance/*"]
    Deps["deps.py::get_current_user<br/>require_admin JWT"]
end

subgraph GRAPH["_004_langgraph_more_nodes"]
    G["graph.py::build_graph()"]
    SQ["standalone_query"]
    IR["intent_recognition"]
    EX["entity_extraction"]
    NZ["entity_normalization"]
    CG["cypher_generation"]
    CE["cypher_executor"]
    DR["doc_retrieval"]
    AG["answer_generation"]
    GR["general_response"]
end

subgraph RAG["common/rag + doc"]
    Mix["doc_store.FaissDocStore.mixed_search<br/>RRF + MMR"]
    Pipe["rag/pipeline.py::retrieve_with_crag"]
    Crag["rag/crag.py::grade"]
    Cite["rag/cite.py"]
    MixKG["doc_rag.py::should_refuse"]
end

subgraph DATA["💾 存储"]
    SQL[("SQLite conversations.sqlite<br/>会话 + kb_* 登记")]
    RD[("Redis List LTRIM 条数")]
    N4[("Neo4j Formula/Herb/…")]
    F1[("FAISS 实体 entities.index")]
    F2[("FAISS 文献 docs.index")]
end

subgraph MEM["🧠 短期记忆 两条裁剪"]
    LTRIM["conversation_store<br/>Redis LTRIM 按条数"]
    CC["context_compressor<br/>token_budget + fit_budget<br/>LLM 摘要 vs 条数截断"]
end

subgraph OBS["📊 横切"]
    LF["langfuse_manager Trace/Span"]
    Deg["obs.py::degraded 永不抛"]
    Gold["eval_golden.py 黄金集"]
    AppLog["app_logging.py data/logs/app.log"]
end

FE --> GW --> GRAPH
GRAPH --> RAG
GRAPH --> DATA
GW --> MEM
GW --> OBS
RAG --> DATA
```

---

## 图 2 · 问答链路（流式，函数级）

```mermaid
flowchart TD
    A(["📥 POST /ask/stream<br/>main.py::ask_stream"]):::start
    A --> AUTH["deps.get_current_user JWT"]
    AUTH --> PRE["main._prepare_turn<br/>store.ensure + add_message user"]
    PRE --> COMP["context_compressor.compress_history<br/>token_budget / fit_budget"]
    COMP --> INIT["main._initial_state<br/>user_question + messages<br/>viewer_dept / viewer_role"]
    INIT --> G["graph.astream_events"]

    G --> SQ["standalone_query<br/>多轮指代 → search_question"]
    SQ --> IR["intent_recognition"]
    IR --> R{_route_by_intent}

    R -->|非中医| GR["general_response"] --> END1(["END"])
    R -->|中医| EX["entity_extraction 六类"]
    EX --> NZ["entity_normalization<br/>FAISS 实体索引"]
    NZ --> CG["cypher_generation<br/>EXPLAIN + 失败 degraded cypher_repair"]
    CG --> CE["cypher_executor Neo4j"]
    CE --> DR["doc_retrieval.make_doc_retrieval_node"]
    DR --> AG["answer_generation 仅此节点推 token"]
    AG --> MIX["doc_rag.should_refuse<br/>图谱空且文献弱 → REFUSE_ANSWER"]
    MIX --> SAVE["main._save_assistant<br/>details.refused / evidence_gap"]
    SAVE --> END2(["✅ session/progress/token/done"]):::start

classDef start fill:#9c3b2e,color:#fff;
```

SSE：`session`（conversation_id）→ `progress`（图节点中文名）→ `token`（仅 `answer_generation` / `general_response`）→ `done`。

---

## 图 3 · 文献检索引擎 mixed_search

实体 FAISS（口语→标准名）与文献 FAISS（知识库块）是**两套文件**。

```mermaid
flowchart TD
    IN(["search_documents(question,<br/>viewer_dept, viewer_role)<br/>doc_store.py"]):::start
    IN --> EN{"DOC_HYBRID_ENABLE"}
    EN -->|1| MIX["FaissDocStore.mixed_search<br/>dense FAISS + BM25 → rag/rrf.py"]
    EN -->|0| DEN["FaissDocStore.search 仅向量"]
    MIX --> MMR{"MMR_ENABLE"}
    MMR -->|1| M1["rag/mmr.py::mmr λ=MMR_LAMBDA"]
    MMR -->|0| KEEP["按 RRF 分截断"]
    DEN --> ACL
    M1 --> ACL
    KEEP --> ACL
    ACL["knowledge_acl.acl_ok<br/>空科室=公开 · admin 全可见"]
    ACL --> BLK["knowledge_service.blocked_ids<br/>过期/不可检索剔除"]
    BLK --> OUT(["→ doc_chunks"]):::start

    OUT --> CRAG["pipeline.retrieve_with_crag"]
    CRAG --> G{crag.grade top1}
    G -->|incorrect + 改写开| RW["_default_rewrite LLM 改写问句<br/>失败 degraded crag_rewrite"]
    RW --> MIX2["再 search 一次"]
    MIX2 --> G2{仍 incorrect?}
    G2 -->|是| REF["hits=[] action=refused"]
    G2 -->|否| OK["action=rewritten"]
    G -->|correct/ambiguous| OK2["action=normal"]
```

图谱事实仍可单独作答：文献 CRAG 拒答不等于整题拒答；`doc_rag.should_refuse` 要求图谱也空。

---

## 图 4 · 短期记忆：LTRIM 条数 vs ContextCompressor token

```mermaid
flowchart TD
    MSG["add_message 落 SQLite"] --> RP["Redis RPUSH + LTRIM<br/>REDIS_HISTORY_MAX=50 按条"]
    ASK["准备进图"] --> HOT["get_recent_for_llm<br/>优先 Redis LRANGE<br/>失败 hydrate SQLite<br/>degraded redis_*"]
    HOT --> CC{"CONTEXT_COMPRESS"}
    CC -->|0| CUT["只留 GRAPH_HISTORY_LIMIT 条"]
    CC -->|1| FIT["fit_budget 从最新往旧<br/>塞进 token_budget"]
    FIT --> OV{有溢出旧轮次?}
    OV -->|无| KEEP["只用 kept"]
    OV -->|有| SUM["LLM 摘要 overflow"]
    SUM -->|失败| FB["degraded context_compress<br/>退回 kept 不中断问答"]
    SUM -->|成功| PACK["_pack_with_summary<br/>摘要+最近轮次仍受 budget 约束"]
```

- **LTRIM**：热缓存条数硬顶，不管 token。  
- **Compressor**：决定塞进模型的历史有多长。`token_budget` 默认 2000（系统提示、图谱结果、当前问句另算）。

---

## 图 5 · 知识库登记（SQLite kb_*）

会话库与知识库共用 `CONVERSATION_DB_PATH`（默认 `data/conversations.sqlite`）。

```mermaid
flowchart LR
    UP["POST /document/upload"] --> ROW["kb_documents pending"]
    ROW --> PARSE["POST /document/parse → kb_chunks"]
    PARSE --> VEC["POST /document/vector/*<br/>status=vectorized<br/>rebuild_index → docs.index"]
    VEC --> ASK["问答 doc_retrieval"]
    RE["同名再上传"] --> VER["kb_document_versions 归档现行"]
    GOV["knowledge-governance profile"] --> META["kb_metadata 有效期/区域"]
    META --> SCAN["scan → kb_issues"]
```

**第一次上传没有版本行**；版本弹窗应展示「现行 vN」。空 `kb_document_versions` 不代表没文档。

---

## 图 6 · 评测飞轮 + 降级打点

```mermaid
flowchart TD
    FB["Chat 👍/👎 → patch_message_details"] --> ADM["GET /admin/feedbacks"]
    ADM --> MK["POST /admin/feedbacks/{id}/golden<br/>eval_golden.add_from_feedback"]
    MK --> JSON["eval/golden_qa.json<br/>source=feedback 允许 expect=[]"]
    JSON --> CLI["scripts/eval_golden.py<br/>关键词全命中才算过<br/>CI 禁止 --live"]
    EX["except 旁路"] --> DEG["obs.degraded(tag) 内存计数永不抛"]
    DEG --> H["GET /health degraded"]
    DEG --> ST["GET /admin/import-status<br/>系统运行状态页"]
```

不是旁路评测子系统，没有知识自进化、没有 llm-user-suite。

---

## 附 A · 配置开关速查（`.env`）

| 域 | 关键开关（默认） |
|---|---|
| 模型 | 默认 `MODEL_*`；聊天下拉 `model_type`：`DEEPSEEK_*` / `QWEN_*`·`DASHSCOPE_*` / `DOUBAO_*`·`ARK_*`。只切问答图；ContextCompressor（`TOKEN_BUDGET` / `fit_budget`，超预算 LLM 摘要）与 Redis `LTRIM` 条数裁剪、长期记忆、评测仍用 `MODEL_*` |
| 图 | `NEO4J_URI` · `NEO4J_USER` · `NEO4J_PASSWORD` |
| 实体向量 | `EMBEDDING_MODEL_PATH` · `FAISS_INDEX_PATH` · `FAISS_METADATA_PATH` |
| 会话 | `REDIS_URL` · `CONVERSATION_DB_PATH` · `REDIS_HISTORY_MAX=50` |
| 记忆 | `TOKEN_BUDGET=2000` · `GRAPH_HISTORY_LIMIT=6` · `CONTEXT_COMPRESS` |
| 文献 RAG | `DOC_RAG_ENABLE=1` · `DOC_HYBRID_ENABLE=1` · `MMR_ENABLE=1` · `CRAG_ENABLE=1` |
| CRAG | `CRAG_HIGH=0.72` · `CRAG_LOW=0.3` · `CRAG_REWRITE_ENABLE=1` |
| 知识库 | `KNOWLEDGE_DOCS_DIR=data/docs` · `DOC_UPLOAD_MAX_BYTES` |
| 评测 | `GOLDEN_QA_PATH=eval/golden_qa.json` |
| 登录 | `JWT_SECRET` · `ADMIN_USERNAME` / `ADMIN_PASSWORD` |
| Langfuse | `LANGFUSE_ENABLED` · `LANGFUSE_HOST` · keys |
| 运行日志 | `LOG_DIR=data/logs`（50MB×10；`TESTING=1` 不写盘） |

---

## 附 B · 端口与部署

| 项 | 值 |
|---|---|
| 前端 Vite | **5174**（strictPort；电网项目占 5173） |
| FastAPI | 8000 · `/api` 由 Vite 反代 |
| Neo4j | 本机 7687（不进本仓库 compose） |
| Redis | 6379（`docker compose up -d redis`） |
| Langfuse | 3000（可选） |
| 默认账号 | admin / admin123 |
| Python | conda 环境名 **`grid-qa`**，不要在仓库建 `venv/` |

---

## 附 C · 闭环总览

> **防胡编**：意图非中医走 `general_response`；图谱空 + 文献 CRAG incorrect → `REFUSE_ANSWER`。  
> **记忆**：Redis 管条数，Compressor 管 token。  
> **回流**：点踩 → 标为黄金集 → 离线关键词命中。  
> **韧性**：依赖失败 `degraded(tag)`，用户侧 fail-open。

**顶层**：网关 JWT 管身份，LangGraph 管图谱问答，`common/rag` 管文献质量，SQLite 管会话与知识登记，obs/Langfuse 管可见性。
